
type User {
  id: ID! @unique
  auth0id: String @unique
}

type ToDo {
  id: ID! @unique
  title: String
  description: String
  isDone: Boolean
}

type Label {
  id: ID! @unique
  name: String
}

type Color {
  id: ID! @unique
  name: String
  colorCode: String
}
